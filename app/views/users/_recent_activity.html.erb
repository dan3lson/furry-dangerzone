<!-- https://www.youtube.com/watch?v=RHXASH7b9_o -->
<% if user.has_any_activity? %>
	<% user.activities.each do |act| %>
		<% next unless act.trackable.present? %>
		<%= render "activities/#{act.trackable_type.underscore}/#{act.action}",
				activity: act
		%>
	<% end %>
<% end %>
