<% provide(:title, @user.username) %>
<% provide(:header, @user.username) %>

<%= render "users/overview_stats", user: @user %>

<%= render "users/fundamentals_game_stats", user: @user %>

<%= render "users/jeopardy_game_stats", user: @user %>

<%= render "users/freestyle_game_stats", user: @user %>
