<% if Rails.env.test? %>
  <%=
    link_to "remove",
    user_word_tag_tag_show_page_path(
      name: word.name,
      definition: word.definition,
      tag_id: tag),
    method: :delete
  %>
<% else %>
  <%=
    link_to image_tag("remove_gray_25.png"),
    user_word_tag_tag_show_page_path(
      name: word.name,
      definition: word.definition,
      tag_id: tag),
    method: :delete
  %>
<% end %>
