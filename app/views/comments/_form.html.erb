<%= form_for [@blog, @comment] do |f| %>
  <%= render "shared/error_messages", object: @comment %>

	<%= hidden_field_tag :blog_url, blog_url %>

  <div>
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <%= f.submit "submit", class: "form-control" %>
<% end %>
