<div>
  <%= form_for(@tag) do |f| %>
    <%= render 'shared/error_messages', object: @tag %>
    <div class="input-group">
      <%= f.label :name, class: "sr-only control-label" %>
      <%= f.text_field :name,
        class: "form-control",
        placeholder: "Name *"
      %>
      <span class="input-group-btn">
        <%= f.button type: "submit", class: "btn btn-primary" do %>
          <i class="fa fa-save"></i>
          Create
        <% end %>
      </span>
    </div>
  <% end %>
</div>
