<% if Rails.env.test? %>
  <%= link_to "remove tag", user_word_tag_word_show_page_path(
      word_id: word, tag_name: tag.name
    ), method: :delete %>
<% else %>
  <%= link_to user_word_tag_word_show_page_path(
    word_id: word, tag_name: tag.name), method: :delete do %>
    <i class="fa fa-remove gray"></i>
  <% end %>
<% end %>
