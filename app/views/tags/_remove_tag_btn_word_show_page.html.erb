<% if Rails.env.test? %>
  <%= link_to "remove tag", user_word_tag_word_show_page_path(
      word_id: word, tag_name: tag.name
    ), method: :delete %>
<% else %>
  <%= link_to image_tag("remove_gray_25.png"),
    user_word_tag_word_show_page_path(word_id: word, tag_name: tag.name),
    method: :delete %>
<% end %>
