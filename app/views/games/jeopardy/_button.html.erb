<button type="button"
				class="btn btn-outline-primary btn-block btn-lg jeop-btn"
				data-word-id="<%= word.id %>"
				data-has-word="<%= current_user.has_word?(word) %>"
				disabled>
	<span><%= word.name %></span>
</button>
