<div class="card mb-3">
  <div class="card-block">
    <h4 class="card-title">
      <%= icon "commenting-o" %> Teacher Review
    </h4>
    <p class="card-text">
      A Teacher will verify the work you did just now and may provide
      feedback. Make sure to come back and check!
    </p>
  </div>
</div>
<div class="card mb-3">
  <div class="card-block">
    <h4 class="card-title">
      <%= icon "gamepad" %> Next Game
    </h4>
    <% if current_user.has_incomplete_not?(uw.word) %>
      <% uw = current_user.rand_incomplete_not(uw.word) %>
      <%= play_btn_big(uw, uw.word.id) %>
    <% else %>
      <p class="card-text">
        You don't have any other incomplete words to play.
        <%= link_to "Go to the Explore page", search_path %> and
        add new words!
      </p>
    <% end %>
  </div>
</div>
