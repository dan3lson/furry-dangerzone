<div>
	<h2 class="basic-apple-blue-link">
		<%= link_to "/blog/#{url_title}" do %>
			<% if icon %>
				<i class="fa fa-<%= icon %>"></i>
				<%= title %>
			<% end %>
		<% end %>
		<br>
		<small><%= date %></small>
	</h2>

	<p class="lead">
		<%= description %>
	</p>
</div>

<hr>
