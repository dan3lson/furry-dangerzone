var $card = $(".card");
var $cardBlock = $card.find(".card-block");
$(".container").find(".alert").remove();
var msgs = JSON.parse("<%= j @msgs.to_json.html_safe %>");
var $resultsPartial = $("<%= j render(partial: 'school/students/create', locals: { results: @msgs }) %>");
$("#new-classroom-student-div").empty().append($resultsPartial);
