<h1>
	Decisions, Decisions
	<br>
	<small class="text-muted">4th Grade</small>
</h1>
<div class="card card-primary text-white mb-3">
	<div class="card-body">
		<h4 class="card-title">Instructions</h4>
		<p class="card-text">
			Create 3-5 questions around each target word by asking students which
			they would prefer between the given alternatives. As you see
			down below, there are four fields to fill in:
			<em>text</em>, <em>choices</em>, <em>answer</em> and <em>feedback</em>.
		</p>
		<p class="card-text">
			Here is an example:
		</p>
		<ol>
			<li>
				<h5>Anticipate</h5>
				<p class="card-text">
					<p class="form-group">
						<%= label_tag "Text", nil, class: "form-control-label" %>
						<%= text_field_tag :text,
															 nil,
															 class: "form-control",
															 placeholder: "Which would you rather anticipate - your birthday or a dentist appointment?",
															 readonly: true %>
					</p>
					<p class="form-group">
						<%= label_tag "Choices", nil, class: "form-control-label" %>
						<%= text_field_tag :choices,
															 nil,
															 class: "form-control",
															 placeholder: "birthday, dentist appointment",
															 readonly: true %>
					</p>
					<p class="form-group">
						<%= label_tag "Answer", nil, class: "form-control-label" %>
						<%= text_field_tag :answer,
															 nil,
															 class: "form-control",
															 placeholder: "birthday",
															 readonly: true %>
					</p>
					<p class="form-group">
						<%= label_tag "Feedback", nil, class: "form-control-label" %>
						<%= text_area_tag :textarea,
															 nil,
															 class: "form-control",
															 placeholder: "Birthdays are events where people have fun and celebrate someone they care about. A dentist appointment, though, usually isn’t something people look forward to because it likely involves discomfort and dealing with bad news like having a cavity.",
															 rows: 6,
															 readonly: true %>
					</p>
				</p>
			</li>
		</ol>
	</div>
</div>

<% @fourth_grade_words.each do |word| %>
	<div class="card mb-3">
		<div class="card-body" data-word-id="<%= word.id %>">
			<div class="float-right">
				<% if word.meaning_alts.count > 1 %>
					<i class="fa fa-check fa-3x text-success"></i>
				<% else %>
					<i class="fa fa-check fa-3x text-muted"></i>
				<% end %>
			</div>
			<h2 class="card-title">
				<%= word.name %>
			</h2>
			<p class="card-text">
				<strong>Part of Speech</strong> <%= word.part_of_speech %>
				<br>
				<strong>Definition</strong> <%= word.definition %>
			</p>
		</div>
		<% if word.has_meaning_alts? %>
			<% word.meaning_alts.each do |meaning_alt| %>
				<div class="card-body" data-meaning-alt-id="<%= meaning_alt.id %>">
					<%= render "school/meaning_alts/card_text", m_a: meaning_alt %>
					<%= render "school/meaning_alts/edit_btn", m_a: meaning_alt %>
				</div>
			<% end %>
		<% end %>
		<% unless word.meaning_alts.count > 1 %>
			<div class="card-footer">
				<%= render "school/meaning_alts/form",
									 word: word,
									 m_a: @meaning_alt,
									 method: :post,
									 btn_text: "Create"
				%>
			</div>
		<% end %>
	</div>
<% end %>
