var $activeElement = $(document.activeElement);
var $partial = $("<%= j render(partial: 'school/meaning_alts/form', locals: { m_a: @m_a, text: 'Update', word: @word }) %>");
var $card = $activeElement.parents(".card");
var $cardBlock = $activeElement.parents(".card");
var $buttons = $card.find(".float-right");
var $form = $activeElement.parent();
var $footer = $form.parent();
$buttons.hide();
$footer.find("p").remove();
$footer.append($partial);
$footer.find(".alert").remove();
