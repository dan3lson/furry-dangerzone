<% provide(:title, "Create a New Word") %>
<% provide(:header, "Create a New Word") %>

<!-- <div>
  <div class="card mb-3">
    <div class="card-body">
      <ol>
        <li>
          Want to add a word, but are unsure if it already exists? Try
          searching for it first.
        </li>
        <li>
          If the exact word you want isn't there, use the form down below to
          create a new word.
        </li>
      </ol>
      <%#= render "searches/form" %>
    </div>
  </div>
  <div class="search-container"></div>
</div> -->
<div class="alert alert-info">
  <%= icon "info-circle" %>
  Complete the form below to create a new word. Then you can create content for
  that word via new forms that appear. Any content you create will be used for
  the Leksi games, which will help students truly learn these new words.
</div>
<div class="row">
  <div class="col-md-6 forms">
    <div class="card bg-light mb-3">
      <div class="card-body">
        <%= form_for([:school, @word], remote: true) do |f| %>
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <%= render 'shared/error_messages', object: @word %>
          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :definition %>
            <%= f.text_field :definition, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :phonetic_spelling, "Phonetic Spelling (Optional)" %>
            <%= f.text_field :phonetic_spelling, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :part_of_speech, "Part of Speech (Optional)" %>
            <%= f.text_field :part_of_speech, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :photo do %>
              <%= f.file_field :photo %>
            <% end %>
          </div>
          <%= f.submit "Create", class: "btn btn-primary btn-block" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
